cd /apps/cheradip/fcheradip
cp src/environments/environment.prod.ts /tmp/environment.prod.ts.backup
# optional: any other file you edit only on the host

git fetch origin
git reset --hard origin/main
# restore server-specific URLs if your backup differs:
cp /tmp/environment.prod.ts.backup src/environments/environment.prod.ts

npm ci
npx ng build --configuration=production

# Angular 16 classic builder: output is dist/frontend/ (no "browser" subfolder).
sudo rsync -av --delete dist/frontend/ /var/www/cheradip/
sudo nginx -t && sudo systemctl reload nginx