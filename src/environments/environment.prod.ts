export const environment = {
    production: true,
    apiUrl: 'https://cheradip.com/api',
    /** Same origin as the site when Angular is served with Django; set origin if media is on another host. */
    backendUrl: ''
  };
  